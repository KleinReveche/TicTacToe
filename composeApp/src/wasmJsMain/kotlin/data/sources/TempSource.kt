package data.sources

import domain.model.AppSetting
import domain.model.GameData
import domain.model.LocalMatch
import domain.model.LocalPlayer

var tempAppSettings: List<AppSetting> = emptyList()
var tempGameData: List<GameData> = emptyList()
var tempLocalMatches: List<LocalMatch> = emptyList()
var tempLocalPlayers: List<LocalPlayer> = emptyList()